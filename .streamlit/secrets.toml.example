# Copy this file to .streamlit/secrets.toml for local testing with Supabase
# On Streamlit Cloud, add these via the Secrets UI (never commit the real file)

DATABASE_URL = "postgresql://postgres:<your-password>@db.<your-project-ref>.supabase.co:5432/postgres"
SAVE_FOLDER = "/tmp"
